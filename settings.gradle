rootProject.name = 'analyse-addons'

// Auto-discover all modules in the modules/ directory
file('modules').eachDir { moduleDir ->
    def moduleName = moduleDir.name
    include "modules:${moduleName}"
    project(":modules:${moduleName}").projectDir = moduleDir
}
